import { FilterAttributesPropsType } from "@/types/components/shop/filters";
import { FC } from "react";

export const FilterAttributes: FC<FilterAttributesPropsType> = ({ attributes }) =>
{
    console.log(attributes);
    return (
        <div>

        </div>
    )
}