import styled from "@emotion/styled";
import { SectionContainer } from "../styles";

export const BlogSectionContainer = styled(SectionContainer)`
  display: flex;
  flex-direction: column;
  row-gap: 48px;
`;
